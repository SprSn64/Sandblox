#define NETWORK_INIT_H
#ifndef NETWORK_INIT_H

#include "server.h"

/*typedef struct{
	Uint8 IPv4[4];
	Uint16 IPv6[8];
} IPAddress;

typedef struct{
	Uint8 serverIP;
	IPAddress* clientsIP;
	Uint16 port;
	char* version;
	float avgPing;
} Server;*/

#endif